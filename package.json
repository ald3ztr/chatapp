{
  "name": "arzudigital-chat",
  "version": "0.1.0",
  "private": true,
  "description": "Arkadaslar arasi, Instagram DM tarzi, gercek zamanli sohbet uygulamasi (mobil oncelikli)",
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c green,cyan \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
